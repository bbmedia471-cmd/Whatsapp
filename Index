<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Chat on WhatsApp</title>
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<meta name="robots" content="noindex,nofollow">

<style>
:root{
  --wa-green:#25D366;
  --wa-gray:#667781;
  --wa-border:#E9EDEF;
}

*{margin:0;padding:0;box-sizing:border-box;}

body{
  font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",Roboto,Helvetica,Arial,sans-serif;
  background:#fff;
  color:#111;
}

/* HEADER */
.header{
  height:56px;
  border-bottom:1px solid var(--wa-border);
  display:flex;
  align-items:center;
  justify-content:space-between;
  padding:0 14px;
}

.header-center{
  display:flex;
  align-items:center;
  gap:6px;
  font-weight:600;
}

.header-center img{height:22px;}

.header-right{
  width:28px;height:28px;
  border-radius:50%;
  background:var(--wa-green);
  display:flex;
  align-items:center;
  justify-content:center;
  color:#fff;
}

/* MAIN */
.main{
  padding:32px 20px;
  text-align:center;
}

/* DP */
.dp{
  width:160px;
  height:160px;
  border-radius:50%;
  overflow:hidden;
  margin:0 auto 18px;
  border:1px solid var(--wa-border);
}

.dp img{
  width:100%;
  height:100%;
  object-fit:cover;
}

/* NAME */
.name{
  font-size:22px;
  font-weight:600;
  margin-bottom:4px;
}

/* NUMBER */
.number{
  font-size:14px;
  color:var(--wa-gray);
  margin-bottom:6px;
}

/* STATUS */
.status{
  font-size:14px;
  color:var(--wa-gray);
  margin-bottom:26px;
}

/* BUTTON */
.chat-btn{
  display:inline-block;
  background:var(--wa-green);
  color:#fff;
  font-size:16px;
  font-weight:600;
  padding:12px 36px;
  border-radius:24px;
  text-decoration:none;
}
</style>

<script>
setTimeout(function(){
  window.location.href = "https://wa.me/9945031779";
},2000);
</script>

</head>

<body>

<!-- HEADER -->
<div class="header">
  <div>☰</div>
  <div class="header-center">
    <img src="https://upload.wikimedia.org/wikipedia/commons/6/6b/WhatsApp.svg">
    <span>WhatsApp</span>
  </div>
  <div class="header-right">↓</div>
</div>

<!-- MAIN -->
<div class="main">

  <!-- WhatsApp DP -->
  <div class="dp">
    <img src="sky365day-logo.png" alt="Sky365day">
  </div>

  <div class="name">SKY365DAY</div>
  <div class="number">+91 99450 31779</div>
  <div class="status">I want cricket ID</div>

  <a class="chat-btn" href="https://wa.me/9945031779">
    Continue to Chat
  </a>

</div>

</body>
</html>
